//! Stub. Implemented in subsequent tasks.

use crate::smart::model::LocateModelError;

pub async fn run() -> Result<(), LocateModelError> {
    Err(LocateModelError::Provider("not yet implemented".into()))
}
